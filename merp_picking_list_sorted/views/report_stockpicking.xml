<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <template id="report_picking_sort_pack_operation_ids" inherit_id="stock.report_picking">
        <xpath expr="//t[contains(@t-as, 'ml')]" position="attributes">
            <attribute name="t-foreach">o.sort_printer_picking_list(move.move_line_ids)</attribute>
        </xpath>
    </template>

</odoo>
